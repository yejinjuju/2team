server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yejin?serverTimezone=Asia/Seoul&characterEncoding=utf-8
    #/db_papawon?serverTimezone=Asia/Seoul&characterEncoding=utf-8 mysql server ????
    username: root
    password: 1234
  thymeleaf:
    cache: false

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update